<div class="wrapper fadeInDown" id="add-product-from">
    <div id="formContent">
        <h2 class="active">Редактиране на публикация</h2>

        <form method="post">
            <input type="text" class="fadeIn second" name="title" placeholder="Заглавие на публикацията" value="{{post.title}}" required>
            <textarea type="text" class="fadeIn third" name="content" placeholder="Съдържание" required>{{post.content}}</textarea>
            <select class="fadeIn third" name="category" id="category" required>
                {{#each categories}}
                    {{#if isSelected}}
                        <option selected value="{{_id}}">{{name}}</option>
                    {{else}}
                        <option value="{{_id}}">{{name}}</option>
                    {{/if}}
                {{/each}}
            </select>
            <input type="submit" class="fadeIn fourth" value="Редактиране">
        </form>
    </div>
</div>